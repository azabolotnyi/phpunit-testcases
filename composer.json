{
    "require-dev": {
        "phpunit/phpunit": "11.*"
    },
    "scripts": {
        "tests": "php -d auto_prepend_file=prepended.php vendor/bin/phpunit -c phpunit.xml",
        "tests-isolated": "php -d auto_prepend_file=prepended.php vendor/bin/phpunit -c phpunit-isolated.xml"
    }
}
